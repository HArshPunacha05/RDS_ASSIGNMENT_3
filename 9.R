# Define the strings
str1 <- "Data"
str2 <- "Science"
str3 <- "R"

# Concatenate the strings with hyphens as separators
concatenated_string <- paste(str1, str2, str3, sep = "-")

# Print the result
cat("Concatenated string:", concatenated_string, "\n")
 

#OUTPUT

#Concatenated string: Data-Science-R 